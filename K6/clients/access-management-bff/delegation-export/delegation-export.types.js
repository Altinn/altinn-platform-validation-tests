// -----------------------------------------------------------------------------
// Query models
// -----------------------------------------------------------------------------

/**
 * Query parameters for {@link GetDelegationExport}.
 *
 * Use {@link GetDelegationExportQueryBuilder} to construct this object.
 *
 * @typedef {object} GetDelegationExportQuery
 * @property {string} [partyUuid] Party UUID to export delegations for.
 * @property {boolean} [includeSubunits] Whether to include the subunits of the
 * party.
 * @property {string} [types] Comma separated list of delegation types.
 * @property {string} [languageCode] Language code of the export, e.g. nb.
 */

export const GetDelegationExportQuery = undefined;
