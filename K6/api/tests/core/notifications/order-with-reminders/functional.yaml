namespace: core
base_dir: K6/api/tests/core/notifications/
test_definitions:
  - test_file: order-with-reminders/for-email-addr.js
    env_file: common.env
    contexts:
      - environment: at22
      - environment: at24
      - environment: yt01

  - test_file: order-with-reminders/for-mobile-number.js
    env_file: common.env
    contexts:
      - environment: at22
      - environment: at24
      - environment: yt01

  - test_file: order-with-reminders/for-orgs.js
    env_file: common.env
    contexts:
      - environment: at22
        test_run:
          secrets:
            - reminders-for-org # resourceId and orgNoRecipient. Are these really secret?
      - environment: at24
        test_run:
          secrets:
            - reminders-for-org # resourceId and orgNoRecipient. Are these really secret?
      - environment: yt01
        test_run:
          secrets:
            - reminders-for-org # resourceId and orgNoRecipient. Are these really secret?

  - test_file: order-with-reminders/for-persons.js
    env_file: common.env
    contexts:
      - environment: at22
        test_run:
          secrets:
            - email-order # Added the ninRecipient, but is this even a secret?
      - environment: at24
        test_run:
          secrets:
            - email-order # Added the ninRecipient, but is this even a secret?
      - environment: yt01
        test_run:
          secrets:
            - email-order # Added the ninRecipient, but is this even a secret?
