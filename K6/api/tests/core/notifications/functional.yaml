namespace: core
base_dir: K6/api/tests/core/notifications/
test_definitions:
  # OLD API
  - test_file: email-request.js
    env_file: common.env
    contexts:
      - environment: at22
        test_run:
          secrets:
            - email-order # Added the ninRecipient, but is this even a secret?
      - environment: yt01
        test_run:
          secrets:
            - email-order # Added the ninRecipient, but is this even a secret?
