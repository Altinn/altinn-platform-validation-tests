namespace: notifications
base_dir: K6/api/tests/notifications/order-with-reminders/
test_definitions:
  - test_file: for-email-addr.js
    contexts:
      - environment: at22
      - environment: yt01

  - test_file: for-mobile-number.js
    contexts:
      - environment: at22
      - environment: yt01

  - test_file: for-orgs.js
    contexts:
      - environment: at22
        test_run:
          secrets:
            - reminders-for-org # resourceId and orgNoRecipient. Are these really secret?

      - environment: yt01
        test_run:
          secrets:
            - reminders-for-org # resourceId and orgNoRecipient. Are these really secret?

  - test_file: for-persons.js
    contexts:
      - environment: at22
        test_run:
          secrets:
            - email-order # Added the ninRecipient, but is this even a secret?

      - environment: yt01
        test_run:
          secrets:
            - email-order # Added the ninRecipient, but is this even a secret?
