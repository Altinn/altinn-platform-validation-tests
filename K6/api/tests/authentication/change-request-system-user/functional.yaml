namespace: authentication
base_dir: K6/api/tests/authentication/change-request-system-user
test_definitions:
  - test_file: create-and-approve-change-request.js
    contexts:
      - environment: at22
      - environment: at23
      - environment: tt02
      - environment: yt01

  - test_file: create-and-delete-change-request.js
    contexts:
      - environment: at22
      - environment: at23
      - environment: tt02
      - environment: yt01

  - test_file: list-change-requests-by-system.js
    contexts:
      - environment: at22
      - environment: at23
      - environment: tt02
      - environment: yt01

  # get-change-requests-by-system-id.js is deliberately not scheduled yet. The
  # endpoint ignores the continuation token it hands out, so the test is red in
  # every environment through no fault of its own, and a permanently red run
  # teaches everyone to ignore the colour. Add the contexts back once
  # Altinn/altinn-authentication#2156 is fixed. Until then it is run by hand and
  # through the folder's run-all.js.
