# Maskinporten schema

`get-maskinporten-schema-delegations.js` reads the maskinporten schema delegations a supplier
has received.

Test data is `access-management/resource-owner/maskinporten-schema/<env>.csv`. Needs
`ENVIRONMENT` and `BASE_URL`.
